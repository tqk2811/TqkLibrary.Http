mode: ManualDeployment
tag-prefix: 'v'
next-version: '1.0'
assembly-versioning-scheme: MajorMinorPatchTag
branches:
  master:
    regex: ^master$
    increment: None
  main:
    regex: ^main$
    increment: None
